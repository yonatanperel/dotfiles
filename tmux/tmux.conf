
set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",xterm-256color:RGB"

# Scrolling
setw -g mouse on

# Start windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1

# Renumber windows
set-option -g renumber-windows on

# Statusline Colors
set -g status-style bg=terminal,fg=terminal

# Command prefix is C-a
unbind C-b
set-option -g prefix C-a

# Split panes with \ and -
bind \\ split-window -h
bind - split-window -v
unbind '"'
unbind %

# reload with r
bind r source-file ~/.config/tmux/tmux.conf

# move betweeen panes
#unbind h
#unbind j
#unbind k
#unbind l
#bind h select-pane -L
#bind j select-pane -D
#bind k select-pane -U
#bind l select-pane -R
#bind -n M-C-h select-pane -L
#bind -n M-C-j select-pane -D
#bind -n M-C-k select-pane -U
#bind -n M-C-l select-pane -R
# Smart pane switching with awareness of Vim splits.
# See: https://github.com/christoomey/vim-tmux-navigator
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|l?n?vim?x?|fzf)(diff)?$'"
bind-key -n 'C-h' if-shell "$is_vim" 'send-keys C-h'  'select-pane -L'
bind-key -n 'C-j' if-shell "$is_vim" 'send-keys C-j'  'select-pane -D'
bind-key -n 'C-k' if-shell "$is_vim" 'send-keys C-k'  'select-pane -U'
bind-key -n 'C-l' if-shell "$is_vim" 'send-keys C-l'  'select-pane -R'
tmux_version='$(tmux -V | sed -En "s/^tmux ([0-9]+(.[0-9]+)?).*/\1/p")'
if-shell -b '[ "$(echo "$tmux_version < 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\'  'select-pane -l'"
if-shell -b '[ "$(echo "$tmux_version >= 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\\\'  'select-pane -l'"

bind-key -T copy-mode-vi 'C-h' select-pane -L
bind-key -T copy-mode-vi 'C-j' select-pane -D
bind-key -T copy-mode-vi 'C-k' select-pane -U
bind-key -T copy-mode-vi 'C-l' select-pane -R
bind-key -T copy-mode-vi 'C-\' select-pane -l

# close pane
unbind c
unbind w
unbind x
bind c kill-pane
bind o switch-client -l
bind x kill-window
bind X kill-window -a
bind w new-window

# back to last pane
bind b last-window

bind f display-popup -E "tmux list-sessions | sed -E 's/:.*$//' | fzf --reverse | xargs tmux switch-client -t"

# bmux
bind A BmuxNewBot
bind S BmuxNewWorktree
bind D BmuxDestroy
bind s BmuxBrowse
bind C-a BmuxJumpAttention
bind a BmuxDismiss

set -g @plugin 'tmux-plugins/tpm'
#set -g @plugin 'spywhere/tmux-now-playing'
#set -g @now-playing-status-format '{scrollable} {icon} [{position}/{duration}]'
#set -g @now-playing-scrollable-threshold 50
#set -g @now-playing-paused-icon ''
#set -g @now-playing-playing-icon '󰐊'
#set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'bmux'

set -g display-time 1000
set -g message-style "fg=#c0caf5,bg=#1a1b26"
set -s escape-time 0

set-option repeat-time 800

set-window-option -g mode-keys vi
bind-key -T copy-mode-vi v send -X begin-selection
bind-key -T copy-mode-vi V send -X select-line
bind-key -T copy-mode-vi y send -X copy-pipe-and-cancel 'pbcopy'

# Green
#set -g window-style 'bg=#1E2A24'
#set -g window-active-style 'bg=#0F1915'
#set -g pane-active-border-style 'fg=#1E2A24'
#set -g pane-border-style 'fg=#1E2A24'

# Blue
#set -g window-style 'bg=#1A2B30'
#set -g window-active-style 'bg=#0D1A21'
#set -g pane-active-border-style 'fg=#1A2B30'
#set -g pane-border-style 'fg=#1A2B30'

# Brown
#set -g window-style 'bg=#2A1A10'
#set -g window-active-style 'bg=#1B0C05'
#set -g pane-active-border-style 'fg=#2A1A10'
#set -g pane-border-style 'fg=#2A1A10'

# Black
#set -g window-style 'bg=#1C1C1C'
#set -g window-active-style 'bg=#0C0C0C'
#set -g pane-active-border-style 'fg=#2A1A10'
#set -g pane-border-style 'fg=#2A1A10'

# None
set -g window-style 'bg=default'
set -g window-active-style 'bg=default'
set -g pane-active-border-style 'fg=green'
set -g pane-border-style 'fg=blue'

set -g pane-border-lines single
set -g pane-border-indicators off
set -g window-status-current-style fg=blue,bold
set -g window-status-current-format '#I:#W '
set -g window-status-format '#I:#W '
set -g status-left "[#{=-50:session_name}] "
set -g status-left-length 100
set -g status-right-length 200

set-option -g status-right "#{bmux_status} | %H:%M"
set -g status-interval 2

set -g pane-border-status top
set -g pane-border-format "#{pane_title}"


# Keep this line at the bottom of the file
run '~/.tmux/plugins/tpm/tpm'
